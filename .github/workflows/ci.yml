# Continuous integration: typecheck + tests against the pinned harness
# commit (HARNESS_COMMIT), the committed-lib build-drift gate, and the
# pack-and-install smoke. Tag pushes that pass also publish to npm when the
# repository owns an NPM_TOKEN secret.
name: ci

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:

jobs:
  test:
    name: typecheck + test + build drift + pack smoke
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.7.0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - name: Read pinned harness commit
        id: harness
        run: echo "commit=$(cat HARNESS_COMMIT)" >> "$GITHUB_OUTPUT"
      - name: Cache harness checkout and build
        id: harness-cache
        uses: actions/cache@v4
        with:
          path: |
            /tmp/dsh-harness
          key: harness-native-${{ steps.harness.outputs.commit }}
      - name: Checkout harness
        if: steps.harness-cache.outputs.cache-hit != 'true'
        run: |
          git clone --filter=blob:none --no-checkout https://github.com/deepseek-ai/deepseek-harness.git /tmp/dsh-harness
          git -C /tmp/dsh-harness checkout "$(cat HARNESS_COMMIT)"
      - name: Install and build harness
        if: steps.harness-cache.outputs.cache-hit != 'true'
        working-directory: /tmp/dsh-harness
        run: |
          corepack enable
          pnpm install --frozen-lockfile
          # The 0.1.5 line locks session files through the native flock addon
          # (native/system/packages/<host>/bin/<libc>/system.node); build:lib
          # does not compile it, so a harness cache built without this step
          # fails every test that touches session persistence with
          # "Cannot find module '.../system.node'".
          pnpm run build:native-system
          pnpm run build:lib
      - name: Install plugin and map harness paths
        run: |
          corepack enable
          pnpm install --frozen-lockfile
          DSH_HARNESS_ROOT=/tmp/dsh-harness pnpm run gen-aliases
      - name: Typecheck
        run: pnpm run typecheck
      - name: Typecheck (published line)
        run: pnpm run typecheck:ci
      # `tests/completeness.spec.ts` asserts over the packaged faces
      # (`lib/index.js`, `lib/client.js`), so the bundles must exist before any
      # vitest step runs. The build-drift gate below still rebuilds and diffs
      # against the committed lib/, so this step cannot hide drift.
      - name: Build (the completeness gate reads the packaged faces)
        run: pnpm run build
      - name: Test
        run: pnpm test
      - name: Coverage
        run: pnpm run test:coverage
      - name: Lint
        run: pnpm run lint
      - name: README sync (five languages)
        run: pnpm run check:readmes
      - name: Verify self-contained dependency specs
        run: pnpm run verify:self-contained
      - name: Verify built artifacts
        run: pnpm run verify:artifacts
      - name: Build drift gate (lib/ must match the committed sources)
        run: |
          pnpm run build
          git diff --exit-code lib/
      - name: Pack smoke
        run: DSH_HARNESS_ROOT=/tmp/dsh-harness node scripts/pack-smoke.mjs

  publish:
    name: publish to npm
    needs: test
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.7.0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org
      # No rebuild here: the test job's build-drift gate already proved the
      # committed lib/ is byte-identical to a fresh build of this tree, and
      # type declarations are not regenerable without the pinned harness.
      # The publish is idempotent: a version already on the registry (e.g. a
      # re-pushed tag or a manual release) skips instead of failing.
      - name: Publish (skips without NPM_TOKEN or an already-published version)
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          corepack enable
          pnpm install --frozen-lockfile
          VERSION="$(node -p "require('./package.json').version")"
          if [ -z "$NODE_AUTH_TOKEN" ]; then
            echo "NPM_TOKEN is not configured on this repository; skipping the npm publish (git install stays the primary channel)"
          elif [ -n "$(npm view "dsh-team-rooms@${VERSION}" version 2>/dev/null)" ]; then
            echo "dsh-team-rooms@${VERSION} already exists on the registry; skipping"
          else
            npm publish
          fi
